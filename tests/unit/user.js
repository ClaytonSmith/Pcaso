var mongoose             = require('mongoose');

describe('Check to ensure the models exist', function(){
    
    before(function(){
	// nothing
    });
    
    it('The `UnauthenticatedUser` model should exist', function(){
	
	//var model = mongoose.model('UnauthenticatedUser');
	//expect( model ).
    });
});
